import mysql.connector

mydb = mysql.connector.connect(
    host = "localhost",
    user = "root",
    password = "***",
    database ="***"
    )

mycursor = mydb.cursor()

sql = "INSERT INTO customers (name,mail) VALUES (%s, %s)"
val = ("Yas**", "yas**@hotmail.com.tr" )
mycursor.execute(sql,val)

mydb.commit()

print(mycursor.rowcount, "recorded.")
